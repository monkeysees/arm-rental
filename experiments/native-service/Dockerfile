# Existing scratch native closure: retained pinned curl, certificates and libraries.
FROM arm-rental-working-set:local
COPY experiments/rust-replay/target/release/rental-replay /usr/local/bin/replay
ENTRYPOINT ["/usr/local/bin/replay"]
